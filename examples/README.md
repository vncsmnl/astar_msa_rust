# Examples

This directory contains example applications and utilities.

## Web Interface

A Flask-based web interface for the MSA algorithms is available in `web-interface/`.

See the web-interface README for setup instructions.

## Running Examples

```bash
# Run Rust examples (when added)
cargo run --example <example_name>
```
